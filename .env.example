# Database Configuration
# Choose your database type: postgresql, mysql, sqlite
DATABASE_TYPE=postgresql

# For PostgreSQL (default)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# For MySQL
# DATABASE_URL=mysql://username:password@localhost:3306/database_name

# For SQLite (local development)
# DATABASE_URL=file:./dev.db

# Alternative PostgreSQL providers
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name (Local)
# DATABASE_URL=postgres://username:password@hostname:5432/database_name (Render)
# DATABASE_URL=postgres://username:password@hostname:5432/database_name (Fly.io)
# DATABASE_URL=postgres://username:password@hostname:5432/database_name (Supabase)
# DATABASE_URL=postgres://username:password@hostname:5432/database_name (Neon)

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-here

# OpenAI Configuration (optional)
OPENAI_API_KEY=your-openai-api-key-here

# Server Configuration
NODE_ENV=development
PORT=5000

# For Render deployment
# DATABASE_URL will be automatically set by Render
# Add your other environment variables in Render dashboard

# For Fly.io deployment
# Set secrets using: flyctl secrets set DATABASE_URL=your_database_url